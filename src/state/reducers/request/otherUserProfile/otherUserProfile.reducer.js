import { createRequestResponseReducer } from '../../generic';
import { otherUserProfileType } from '../../../types/listener/listener';

export default createRequestResponseReducer(otherUserProfileType);
