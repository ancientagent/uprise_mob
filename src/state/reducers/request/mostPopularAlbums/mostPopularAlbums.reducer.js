import { createRequestResponseReducer } from '../../generic';
import { mostPopularAlbumsType } from '../../../types/listener/listener';

export default createRequestResponseReducer(mostPopularAlbumsType);
