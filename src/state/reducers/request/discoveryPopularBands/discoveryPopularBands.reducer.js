import { createRequestResponseReducer } from '../../generic';
import { discoveryPopularBandsType } from '../../../types/listener/listener';

export default createRequestResponseReducer(discoveryPopularBandsType);
