import { createRequestResponseReducer } from '../../generic';
import { bandmembersLlistType } from '../../../types/listener/listener';

export default createRequestResponseReducer(bandmembersLlistType);
