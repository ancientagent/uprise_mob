import { createRequestResponseReducer } from '../../generic';
import { deleteFollowersType } from '../../../types/listener/listener';

export default createRequestResponseReducer(deleteFollowersType);
