import { createRequestResponseReducer } from '../../generic';
import { avaliableCitiesType } from '../../../types/listener/listener';

export default createRequestResponseReducer(avaliableCitiesType);
