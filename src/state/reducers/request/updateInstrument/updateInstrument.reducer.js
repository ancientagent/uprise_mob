import { createRequestResponseReducer } from '../../generic';
import { updateInstrumentType } from '../../../types/listener/listener';

export default createRequestResponseReducer(updateInstrumentType);
