import { createRequestResponseReducer } from '../../generic';
import { mostPopularBandsType } from '../../../types/listener/listener';

export default createRequestResponseReducer(mostPopularBandsType);
