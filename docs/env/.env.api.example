# API service environment template (sanitized)
# Copy to ../webapp_api/.env and fill CHANGEME values

# Postgres
DB_HOST=localhost
DB_PORT=5433
DB_NAME=uprise
DB_USER=CHANGEME
DB_PASSWORD=CHANGEME

# PostGIS extensions expected: postgis, uuid-ossp, pgcrypto

# JWT / Auth
JWT_SECRET=CHANGEME
ACCESS_TOKEN_TTL=15m
REFRESH_TOKEN_TTL=30d

# Server
PORT=4000
NODE_ENV=development
